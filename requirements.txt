# Omega Universal OS - Python Dependencies
# Core dependencies are minimal to keep the system lightweight

# No external dependencies required for core functionality
# All modules use Python standard library

# Optional development dependencies (install with: pip install -e ".[dev]")
# pytest>=7.0.0
# pytest-cov>=4.0.0
# black>=22.0.0
# flake8>=4.0.0
